---
import BaseLayout from './BaseLayout.astro';
import { tenantIdFromRequest } from '../lib/auth';
import { getTenantById, safeTenant } from '../lib/store';

interface Props {
  title?: string;
  active?: 'bahia' | 'servicios' | 'clientes' | 'perfil';
}

const { title = 'Panel', active = 'bahia' } = Astro.props;
const tenantId = tenantIdFromRequest(Astro.request);

if (!tenantId) {
  return Astro.redirect('/login');
}

const tenant = await getTenantById(tenantId);
if (!tenant) {
  return Astro.redirect('/login');
}

const t = safeTenant(tenant);
const siteUrl = (import.meta.env.PUBLIC_SITE_URL || 'https://citas.renace.tech').replace(/\/$/, '');
const publicUrl = `${siteUrl}/s/${t.slug}`;

const nav = [
  { id: 'bahia', href: '/app', label: 'Bahía', icon: '◈' },
  { id: 'servicios', href: '/app/servicios', label: 'Servicios', icon: '✂' },
  { id: 'clientes', href: '/app/clientes', label: 'Clientes', icon: '◎' },
  { id: 'perfil', href: '/app/perfil', label: 'Perfil', icon: '✦' },
] as const;
---

<BaseLayout title={`${title} · ${t.businessName}`} bodyClass="">
  <div class="min-h-dvh">
    <header class="sticky top-0 z-40 border-b border-border bg-surface/95 backdrop-blur-md">
      <div class="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-3">
        <div class="min-w-0">
          <p class="brand-mark text-xl text-gold">{t.businessName}</p>
          <a class="truncate text-xs text-muted hover:text-gold" href={publicUrl} target="_blank" rel="noreferrer">
            {publicUrl.replace('https://', '')}
          </a>
        </div>
        <nav class="hidden items-center gap-1 sm:flex">
          {nav.map((item) => (
            <a
              href={item.href}
              class:list={[
                'rounded-md px-3.5 py-2 text-sm font-semibold uppercase tracking-wide transition',
                active === item.id
                  ? 'bg-gold-dim text-gold'
                  : 'text-ink-soft hover:text-gold',
              ]}
            >
              {item.label}
            </a>
          ))}
          <button id="logout-btn" type="button" class="btn-ghost ml-2 !py-2 !text-xs">Salir</button>
        </nav>
      </div>
      <nav class="flex gap-1 overflow-x-auto border-t border-border px-2 py-2 sm:hidden">
        {nav.map((item) => (
          <a
            href={item.href}
            class:list={[
              'chip whitespace-nowrap',
              active === item.id ? 'bg-gold text-void' : 'border border-border text-ink-soft',
            ]}
          >
            {item.icon} {item.label}
          </a>
        ))}
      </nav>
    </header>

    <main class="mx-auto max-w-6xl px-4 py-6">
      <slot />
    </main>
  </div>

  <script>
    document.getElementById('logout-btn')?.addEventListener('click', async () => {
      await fetch('/api/auth/logout', { method: 'POST' });
      location.href = '/login';
    });
  </script>
</BaseLayout>
